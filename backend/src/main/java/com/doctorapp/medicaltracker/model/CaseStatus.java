package com.doctorapp.medicaltracker.model;

public enum CaseStatus {

    OPEN,         // when case is first created
    IN_PROGRESS,  //when treatement has started
    RESOLVED,     //when treatment is completed
    CLOSED        //when case is archived
                            
}
